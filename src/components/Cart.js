import { useCallback, useContext, useState } from "react";
import MiCostumProvider from "/MiCostumProvider";

const Cart = () => {
  <div>
    <h1>Carrito</h1>
    <h2>Precio Total: ${MiCostumProvider}</h2>
    <Cart />
  </div>;
};

export default Cart;
